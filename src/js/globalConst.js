var searchText = '';
var whichTypeMovieSearch = 'trend'; // "trend" | "search" | "library" | "watched"
var totalPages = 1;

const gloalVar = { searchText, whichTypeMovieSearch, totalPages };
export default gloalVar;
