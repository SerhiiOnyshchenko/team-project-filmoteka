var searchText = '';
var whichTypeMovieSearch = 'trend'; // "trend" | "search" | "library" | "watched" | "author" | "genres"
var totalPages = 1;
var personId = 0;
var genre = '';

const gloalVar = {
   searchText,
   whichTypeMovieSearch,
   totalPages,
   personId,
   genre,
};
export default gloalVar;
