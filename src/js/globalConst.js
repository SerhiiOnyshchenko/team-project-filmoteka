var searchText = '';
var whichTypeMovieSearch = 'trend'; // "trend" | "search" | "library" | "watched" | "author"
var totalPages = 1;
var personId = 0;

const gloalVar = { searchText, whichTypeMovieSearch, totalPages, personId };
export default gloalVar;
